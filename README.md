# Google Apps Script: Create Events from Google Sheet
This script reads data from a Google Sheet and creates events in a Google Calendar.

## Configuration
Create a `config.js` file with the following configuration: